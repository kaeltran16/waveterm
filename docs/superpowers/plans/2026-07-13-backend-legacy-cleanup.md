# Backend Legacy Cleanup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remove Electron-era and multi-block/multi-tab backend code that no longer has a live caller in the single-pane Tauri agent-cockpit.

**Architecture:** This is a deletion effort, not a feature. Every target below was confirmed by cross-referencing the backend definition against all callers (frontend `frontend/`, wsh CLI `cmd/wsh/`, Go packages, and `src-tauri/*.rs`). "Dead" means: only the definition + generated binding + (sometimes) a server impl exist, with zero live invocation. The wire protocol is generated from Go, so command/service removals require regenerating bindings via `task generate` — never hand-edit generated files.

**Tech Stack:** Go (wavesrv/wsh), Rust (Tauri shell), TypeScript/React (frontend), Task (build orchestration), zig (CGO compiler).

## Global Constraints

- **Go is the source of truth for the wire protocol.** After changing any wshrpc / waveobj / wconfig / service type, run `task generate` and commit the regenerated files (`frontend/app/store/wshclientapi.ts`, `frontend/app/store/services.ts`, `frontend/types/gotypes.d.ts`, etc.) together with the Go change. Never hand-edit generated files.
- **`npx tsc` stack-overflows on this repo.** Typecheck with `node --stack-size=4000 node_modules/typescript/lib/tsc.js --noEmit`. Baseline is exit 0 — any error it reports is yours.
- **Go sqlite tests need CGO+zig.** Run Go tests as `CGO_ENABLED=1 CC="zig cc -target x86_64-windows-gnu" go test ./pkg/...`.
- **Do NOT touch `secretstore`.** It stores live user data (AI tokens, SSH passwords, waveapp secrets) and is runtime-broken under Tauri (encrypt/decrypt route to the dead "electron" route). Rewiring it to a Go/Tauri-native crypto path is a separate follow-up, not cleanup. Consequently, `ElectronEncryptCommand` and `ElectronDecryptCommand` (`wshrpctypes.go`) and `CommandElectronEncryptData`/`CommandElectronDecryptData` MUST be preserved — secretstore is their only remaining caller.
- **Do NOT delete the `aifilediff` view.** It is AI-diff functionality (registered, has a preview harness + passing test) that reads as not-yet-wired future work for the agent cockpit, not terminal-era legacy. Leave it and its `WaveAIGetToolDiffCommand` backend intact.
- **Do NOT delete the `vdom` view or its backend.** It renders the terminal's toolbar via `SubBlock` (`term-wsh.tsx`, `term.tsx`). Removing it breaks live terminals.
- **Commits require explicit user approval** (per project git rules). Do the work per-task; present the batched diff and proposed commit message for approval rather than auto-committing.

### Standard Verification (referenced by every task as "run Standard Verification")

Run in order; every step must be green before the task is complete:

1. If any `*.go` type/interface/service changed: `task generate` (regenerates TS+Go bindings).
2. Backend build: `task build:backend` (builds wavesrv + wsh into `dist/bin/`; must exit 0).
3. Go tests: `CGO_ENABLED=1 CC="zig cc -target x86_64-windows-gnu" go test ./pkg/...` (must pass).
4. Frontend typecheck: `node --stack-size=4000 node_modules/typescript/lib/tsc.js --noEmit` (must exit 0).
5. Frontend tests: `npx vitest run` (must pass).
6. For tasks flagged **BOOT-VERIFY**: with `task dev` running, `node scripts/cdp-shot.mjs` and confirm the cockpit renders (terminal mounts, no console errors) — the change touched a live boot/render path.

---

## File Structure

No new files. All changes are deletions or reductions in existing files:

- `pkg/wshrpc/wshrpctypes.go` — the command interface (remove dead command declarations).
- `pkg/wshrpc/wshserver/wshserver.go` — command implementations (remove impls for deleted commands).
- `pkg/wshrpc/wshclient/wshclient.go` — **generated**; regenerated by `task generate`.
- `pkg/service/{clientservice,objectservice,windowservice,workspaceservice}/*.go` — remove dead methods.
- `pkg/wshrpc/wshremote/processviewer.go` — delete (serves orphaned processviewer view).
- `pkg/blockcontroller/{blockcontroller.go,tsunamicontroller.go}` — remove tsunami controller dispatch + file.
- `pkg/eventbus/eventbus.go`, `pkg/wcore/{window.go,workspace.go,layout.go}` — remove electron-event + starter-layout dead code.
- `pkg/wconfig/{settingsconfig.go,metaconsts.go}` — remove dead config keys.
- `pkg/telemetry/telemetry.go` — remove dead multi-window/tab fields.
- `pkg/wavebase/wavebase.go` — remove `WAVETERM_ELECTRONEXECPATH` plumbing (pending Task 5 verification).
- `cmd/wsh/cmd/{wshcmd-badge.go,wshcmd-tabindicator.go,wshcmd-notify.go,wshcmd-web.go,wshcmd-version.go}` — remove/adjust broken electron-route CLI subcommands.
- `cmd/server/main-server.go` — remove NetworkOnline diagnostic-ping gate.
- `frontend/app/view/{preview,sysinfo,processviewer,waveconfig,quicktipsview}/` — delete orphaned view dirs.
- `frontend/app/block/{blockregistry.ts,blockutil.tsx}` — remove registry/util entries for deleted views.

---

## Task 1: Remove dead zero-caller wshrpc commands

Confirmed dead (only declaration + generated binding + sometimes impl; no live caller anywhere):

**Files:**
- Modify: `pkg/wshrpc/wshrpctypes.go`
- Modify: `pkg/wshrpc/wshserver/wshserver.go` (remove matching impls)
- Regenerated: `pkg/wshrpc/wshclient/wshclient.go`, `frontend/app/store/wshclientapi.ts`

**Remove these command declarations** (verify each still has zero live caller with a repo-wide grep on the method name AND its wire string before deleting):
- `DisposeCommand` (:43), `MessageCommand` (:50), `EventUnsubAllCommand` (:67), `FileRestoreBackupCommand` (:70), `GetTempDirCommand` (:71), `ActivityCommand` (:91), `GetTabCommand` (:150), `UpdateTabNameCommand` (:151), `DismissWshFailCommand` (:165), `NotifySystemResumeCommand` (:169), `GetFocusedBlockDataCommand` (:223), `SetChannelGatekeeperCommand` (:123)

**Keep** (they have live callers): everything not listed. In particular keep `EventUnsubCommand`, `SetChannelTierCommand`, `WaveAIGetToolDiffCommand`, `WaveAIAddContextCommand`, `RecordTEventCommand`, `WshActivityCommand`.

**Note on `StreamCpuDataCommand`/`RemoteStreamCpuDataCommand`:** deferred to Task 4 (they belong with the sysinfo view removal).

- [ ] **Step 1:** For each command above, grep repo-wide for the method name and its lowercased wire string (e.g. `"dispose"`, `"gettab"`). Confirm zero hits outside `wshrpctypes.go`, `wshclient.go`, `wshclientapi.ts`, `wshserver.go`, and mock/doc files. If a live caller is found, remove that command from this task and note it.
- [ ] **Step 2:** Delete the interface declaration line(s) in `wshrpctypes.go` for each confirmed command.
- [ ] **Step 3:** Delete the corresponding `func (*WshServer) <Name>(...)` implementation in `wshserver.go` (grep for the method name in that file). Remove any now-unused helper/type only used by that impl.
- [ ] **Step 4:** Run `task generate`.
- [ ] **Step 5:** Run Standard Verification (steps 1-5).
- [ ] **Step 6:** Present diff + proposed message `refactor(wshrpc): remove dead zero-caller commands` for commit approval.

**Risk:** Low. Pure dead-code removal. The only trap is a command that looks dead but is dispatched by wire string — Step 1's string grep guards against this.

---

## Task 2: Remove the old WaveAI chat backend

The standalone WaveAI chat block is orphaned; the cockpit uses the aiusechat/jarvis path.

**Files:**
- Modify: `pkg/wshrpc/wshrpctypes.go`, `pkg/wshrpc/wshserver/wshserver.go`
- Regenerated bindings.

**Remove:** `AiSendMessageCommand` (:210), `WaveAIEnableTelemetryCommand` (:211), `GetWaveAIChatCommand` (:212), `GetWaveAIRateLimitCommand` (:213), `WaveAIToolApproveCommand` (:214).

**Keep:** `WaveAIGetToolDiffCommand` (:216, used by aifilediff) and `WaveAIAddContextCommand` (:215, used by `wsh ai`).

- [ ] **Step 1:** Grep each removed command name + wire string repo-wide; confirm zero live callers (expect only the standalone WaveAI chat view, if any remains, plus generated files). If a WaveAI chat *view* still exists in `frontend/app/view/`, confirm it is not mounted by the cockpit (`Grep` in `frontend/app/{cockpit,view/agents}`); if unmounted, add its dir to the deletion.
- [ ] **Step 2:** Delete the five declarations and their impls; remove now-orphaned request/response types only used by them (grep each type name).
- [ ] **Step 3:** `task generate`.
- [ ] **Step 4:** Run Standard Verification (1-5).
- [ ] **Step 5:** Present diff + `refactor(wshrpc): remove orphaned WaveAI chat backend` for approval.

**Risk:** Low-medium. Trap: shared request/response types also used by the kept WaveAI commands — only delete a type if grep shows it is exclusive to a removed command.

---

## Task 3: Remove dead service methods

HTTP service methods with zero callers (frontend + `src-tauri/*.rs` both checked).

**Files:**
- Modify: `pkg/service/clientservice/clientservice.go`, `objectservice/objectservice.go`, `windowservice/windowservice.go`, `workspaceservice/workspaceservice.go`
- Regenerated: `frontend/app/store/services.ts`

**Remove:**
- clientservice: `FocusWindow` (:49), `TelemetryUpdate` (:69). (`GetTab` and `AgreeTos` are handled in Task 6 with the starter-layout chain since `AgreeTos` pulls in `BootstrapStarterLayout`.)
- objectservice: `GetObjects` (:60), `CreateBlock` (:82), `DeleteBlock` (:104) — FE uses the wshrpc equivalents.
- windowservice: `SetWindowPosAndSize` (:60), `SwitchWorkspace` (:89), `CloseWindow` (:109). **Keep** `GetWindow`, `CreateWindow` (live via `bootids.ts`).
- workspaceservice: `CreateWorkspace` (:31), `UpdateWorkspace` (:45), `DeleteWorkspace` (:92), `ListWorkspaces` (:116), `GetColors` (:135), `GetIcons` (:145), `SetActiveTab` (:173). **Keep** `GetWorkspace`, `CreateTab`, `CloseTab`.

- [ ] **Step 1:** For each method, grep `frontend/` for `.<Method>(` on the service object and `callBackendService(..., "<Method>")`, and grep `src-tauri/` — confirm zero. Removing a method that a wshrpc command routes to is out of scope; verify the method is not invoked by any `wshserver` impl either.
- [ ] **Step 2:** Delete each confirmed method. If a `wcore.*` helper becomes unused after removing the method that called it, remove it too (grep the helper name) — but defer window-geometry `wcore` helpers to Task 5.
- [ ] **Step 3:** `task generate` (regenerates `services.ts`).
- [ ] **Step 4:** Run Standard Verification (1-5).
- [ ] **Step 5:** Present diff + `refactor(service): remove dead multi-window/workspace service methods` for approval.

**Risk:** Medium. `services.ts` is generated and consumed broadly; the typecheck (Standard Verification step 4) catches any missed reference.

---

## Task 4: Remove orphaned view types and their dedicated backends

Views with no cockpit mount path, plus the backend code that served only them.

**Frontend deletes:**
- `frontend/app/view/preview/`, `frontend/app/view/sysinfo/`, `frontend/app/view/processviewer/`, `frontend/app/view/waveconfig/`, `frontend/app/view/quicktipsview/`
- `frontend/app/block/blockregistry.ts` — remove the `BlockRegistry.set(...)` lines for `preview`, `sysinfo`, `cpuplot`, `processviewer`, `waveconfig`, `tips` and their imports.
- `frontend/app/block/blockutil.tsx` — remove icon/label entries for those view types.

**Backend deletes:**
- `pkg/wshrpc/wshremote/processviewer.go` (entire file — serves processviewer only).
- `pkg/wshrpc/wshserver/wshserver.go` — the `MakePlotData`/`SavePlotData` sysinfo/cpuplot plot-data handlers (~:125-154).
- `pkg/wshrpc/wshrpctypes.go` — `StreamCpuDataCommand` (:74), `RemoteStreamCpuDataCommand` (:176), and any process-table commands (`GetProcessTable`/`WatchProcess`-style) used only by processviewer. Remove matching impls.
- `pkg/blockcontroller/tsunamicontroller.go` (entire file) + the `tsunami` dispatch case in `pkg/blockcontroller/blockcontroller.go` (~:274-276) and the `TsunamiController` const (~:36). **Verify first** that no cockpit path sets `controller:"tsunami"` and that tsunami-as-a-widget-app is not otherwise launched (grep `"tsunami"` in `pkg/` + `frontend/`).

**Do NOT delete:** the generic fileshare/`wshremote` file-streaming RPCs (used by the agents `filessurface`/gitdiff), the `vdom` view/backend, or the `aifilediff` view.

- [ ] **Step 1:** Confirm zero cockpit coupling: `Grep` for `PreviewModel|Sysinfo|ProcessViewer|WaveConfigViewModel|QuickTips` scoped to `frontend/app/{cockpit,view/agents}/**` → expect no files.
- [ ] **Step 2:** Delete the frontend view dirs and their `blockregistry.ts` / `blockutil.tsx` entries. Delete any orphaned `.preview*`/test files under `frontend/preview/previews/` that reference only removed views.
- [ ] **Step 3:** Delete the backend files/handlers/commands listed above (including the two CPU-stream commands from Task 1's deferral). Verify the tsunami controller has no launch path before removing.
- [ ] **Step 4:** `task generate`.
- [ ] **Step 5:** Run Standard Verification (1-6, **BOOT-VERIFY** — this touches the block registry and render path; confirm the cockpit terminal still mounts).
- [ ] **Step 6:** Present diff + `refactor(views): remove orphaned terminal-era block views and backends` for approval.

**Risk:** Medium-high. Traps: (1) a removed view's backend RPC is also generic (only remove processviewer-exclusive ones); (2) `blockregistry` default-model fallback must stay so `SubBlock`/vdom keep working — do not remove the registry indirection itself, only the orphaned entries; (3) tsunami may be a widget-app launch path, not just a block controller — verify before deleting.

---

## Task 5: Remove the dead electron-route command cluster

Commands routed to `wshutil.ElectronRoute` (`"electron"`) that nothing services under Tauri (verified: only senders exist, no route handler). Each currently dead-ends at a timeout.

**Remove commands + impls** (`wshrpctypes.go` + `wshserver.go` if impl exists):
- `WebSelectorCommand` (:188), `NotifyCommand` (:189), `FocusWindowCommand` (:190), `NetworkOnlineCommand` (:191-ish), `ElectronSystemBellCommand`, `GetUpdateChannelCommand` (:203).
- **Keep** `ElectronEncryptCommand`/`ElectronDecryptCommand` + `CommandElectronEncryptData`/`CommandElectronDecryptData` (secretstore depends on them — see Global Constraints).

**Remove/adjust the now-dead callers:**
- `cmd/wsh/cmd/wshcmd-badge.go` and `wshcmd-tabindicator.go` — the `ElectronSystemBellCommand` calls; if the whole subcommand exists only to call it, remove the subcommand (and its cobra registration).
- `cmd/wsh/cmd/wshcmd-notify.go` — `NotifyCommand`; remove the subcommand (native notifications are gone).
- `cmd/wsh/cmd/wshcmd-web.go` — `WebSelectorCommand`; the `web` view no longer exists, so remove the subcommand.
- `cmd/wsh/cmd/wshcmd-version.go:49` — `GetUpdateChannelCommand`; drop the update-channel line from version output (there is no auto-updater under Tauri).
- `cmd/server/main-server.go:163` — the `NetworkOnlineCommand` gate on `sendDiagnosticPing`. Read the surrounding function; the gate always fails now (offline). Simplest correct change: remove the gate and either run the ping unconditionally or remove the diagnostic ping if it exists only for this gate. Decide by reading the function; document which you chose.
- `pkg/wcore/window.go:45` — `FocusWindowCommand` inside `SwitchWorkspace`. Since `windowservice.SwitchWorkspace` is removed in Task 3, remove the now-dead `wcore.SwitchWorkspace`/`FocusWindow` multi-window helpers (`window.go:36-48,194-209`) too. Verify no other caller first.

**Remove electron eventbus plumbing** (output is swallowed by `main.rs:56`):
- `pkg/eventbus/eventbus.go` — `SendEventToElectron` (:49-57) and the `WSEvent_ElectronCloseWindow`/`WSEvent_ElectronUpdateActiveTab` consts (:15-16).
- `pkg/wcore/workspace.go:370-371` and `pkg/wcore/window.go:162-166` — the `SendEventToElectron(...)` call sites.

**`WAVETERM_ELECTRONEXECPATH`** (`pkg/wavebase/wavebase.go`): verify who sets it now. `GetWaveAppElectronExecPath` is consumed by `buildercontroller.go:212` and `tsunamicontroller.go:169` (the latter deleted in Task 4). If nothing in `src-tauri/` exports the env var and buildercontroller is the only remaining consumer, decide with the user whether builder/tsunami apps are still a supported path before removing — otherwise leave it and note it.

- [ ] **Step 1:** Re-confirm no `"electron"` route handler exists (grep `ElectronRoute`/`"electron"` for a *receiver* registration, not just senders).
- [ ] **Step 2:** Remove the six commands + impls (keeping Encrypt/Decrypt). `task generate`.
- [ ] **Step 3:** Remove/adjust each caller listed above. Build wsh (`task build:backend`) after CLI subcommand removals to catch dangling cobra registrations.
- [ ] **Step 4:** Remove the eventbus electron plumbing and its two call sites.
- [ ] **Step 5:** Resolve `WAVETERM_ELECTRONEXECPATH` per the verification note (remove or leave-with-note).
- [ ] **Step 6:** Run Standard Verification (1-6, **BOOT-VERIFY** — `main-server.go` and `wcore` are boot-path; confirm wavesrv starts and the cockpit boots).
- [ ] **Step 7:** Present diff + `refactor(electron): remove dead electron-route command cluster` for approval.

**Risk:** High. This is the largest blast radius: wsh CLI surface changes, boot-path code (`main-server.go`), and `wcore` window helpers. Do this task alone, not batched with others. BOOT-VERIFY is mandatory.

---

## Task 6: Remove the dead starter-layout chain and dead config/telemetry

**Starter-layout dead chain** (confirmed dormant: `AgreeTos` has zero frontend callers, so `BootstrapStarterLayout` never runs and `GetStarterLayout` — which builds the legacy term/sysinfo/web/preview 4-block split — is dead):
- `pkg/service/clientservice/clientservice.go` — remove `AgreeTos` (:53) and `GetTab` (:32).
- `pkg/wcore/layout.go` — remove `GetStarterLayout` (:35-61) and `BootstrapStarterLayout` (:148-175). **Keep** `GetNewTabLayout` and `ApplyPortableLayout`/`QueueLayoutAction` (live per-tab creation). If, after removal, split/replace layout action types (`SplitHorizontal`/`SplitVertical`/`InsertAtIndex`/`Replace`) have no remaining caller, remove them too — but `CleanupOrphaned` is still wired (`blockservice.go:77-88`), keep it. Grep each action type before removing.

**Dead config keys** (`pkg/wconfig/settingsconfig.go` + `pkg/wconfig/metaconsts.go`) — remove only keys the frontend never reads (grep each key string in `frontend/` first):
- Auto-updater: `autoupdate:enabled|intervalms|installonquit|channel`.
- Native chrome / menu: `window:showmenubar`, `window:nativetitlebar`, `window:disablehardwareacceleration`, `window:savelastwindow`, `window:dimensions`.
- **Verify before removing** (the FE may still honor these for the tiled layout): `window:zoom`, `window:opacity`, transparency/blur keys, `app:globalhotkey`, `app:confirmquit`, `app:tabbar`, `window:maxtabcachesize`, `window:fullscreenonlaunch`. Only remove a key if grep shows zero frontend reads; otherwise leave and note.

**Dead telemetry fields** (`pkg/telemetry/telemetry.go`) — remove only if nothing populates them anymore (the FE counters feeding them may be gone; grep the JSON tag in `frontend/` and the field name in `pkg/`): `NumWindows`, `NumTabs`. Leave `NumBlocks`/`NumMagnify`/`Displays`/`Renderers` unless confirmed dead.

- [ ] **Step 1:** Confirm `AgreeTos` has no frontend caller (already verified) and `BootstrapStarterLayout` has no other caller. Remove the starter-layout chain.
- [ ] **Step 2:** For each config key, grep its exact string in `frontend/`; remove only zero-hit keys from both `settingsconfig.go` and `metaconsts.go`. Leave and note any with FE reads.
- [ ] **Step 3:** For `NumWindows`/`NumTabs`, confirm no populator remains; remove if dead.
- [ ] **Step 4:** `task generate` (config + telemetry types are generated).
- [ ] **Step 5:** Run Standard Verification (1-6, **BOOT-VERIFY** — config schema drives settings load; confirm the cockpit boots and settings surface renders).
- [ ] **Step 6:** Present diff + `refactor(config): remove dead starter-layout, config keys, and telemetry fields` for approval.

**Risk:** Medium-high. Config schema changes can break settings load if a key is still read. The per-key grep gate + BOOT-VERIFY are the safeguards. When unsure about a key, leave it — false-negative (leaving a dead key) is cheap; false-positive (removing a live key) breaks the app.

---

## Follow-ups (out of scope for this cleanup — report to user)

- **secretstore rewire.** `pkg/secretstore` is runtime-broken under Tauri: it delegates encrypt/decrypt to the dead `"electron"` route, so `secrets.enc` writes time out and are silently dropped (in-memory secrets work per-session but never persist). It backs live features (AI API tokens, SSH passwords, waveapp secrets, the secrets RPC). Needs a Go/Tauri-native crypto path (OS keychain or local key file). Track separately.
- **aifilediff view wiring.** Registered but not mounted in the cockpit; confirm whether it is intended for agent file-diff display and wire it, or remove it, in a deliberate decision.
- **LayoutState model reduction.** `Tab.LayoutState` (RootNode/MagnifiedNodeId/LeafOrder/…) is largely unexercised by the single pane but is structurally created per tab by `ApplyPortableLayout`. Reducing it requires reworking tab creation — larger than cleanup, defer.

---

## Self-Review Notes

- **Scope coverage vs recon:** Section A (dead commands) → Tasks 1, 2, 4 (CPU), 5. Section B (dead service methods) → Task 3, 6. Section C (electron/multi-pane remnants) → Tasks 5, 6. Orphaned views → Task 4. Config/telemetry/starter-layout → Task 6. secretstore → Follow-ups (per user decision to investigate-then-decide; investigation concluded: keep + rewire separately).
- **Preserved-throughout invariants:** secretstore + Electron Encrypt/Decrypt commands; vdom view+backend; aifilediff view; blockregistry indirection; generic fileshare RPCs; `GetNewTabLayout`/`CleanupOrphaned`.
- **Ordering rationale:** safe pure-deletions first (1-3), then medium view removal (4), then the high-blast-radius electron cluster alone (5), then schema/bootstrap (6). Each task is independently buildable, testable, and committable.
